#!/bin/bash

APPNAME="$1"
TAG="$2"
BUILDFLAGS="$3"
GOOS="$4"
GOARCH="$5"
EXT=$6
shift 6
OPTIONS=$*

# shellcheck disable=2086
go build -o "${BUILDFLAGS}" "${APPNAME}-${TAG}-${GOOS}-${GOARCH}${EXT}" ${OPTIONS}